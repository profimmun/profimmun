/**
 * Реквизиты оператора персональных данных и версии правовых документов.
 *
 * ⚠️ ЗАПОЛНИТЕ РЕАЛЬНЫМИ ДАННЫМИ ПЕРЕД ЗАПУСКОМ.
 * Пока значения помечены как «не указано», на правовых страницах выводится
 * заметное предупреждение — чтобы платформа не ушла в продакшен с заглушками.
 *
 * Реквизиты обязательны по ст. 18.1 152-ФЗ (оператор должен быть определён)
 * и по ст. 10 Закона «О защите прав потребителей» (сведения об исполнителе).
 */

export const NOT_SET = "не указано";

export const legal = {
  /** Полное наименование: ООО «…», ИП Фамилия И. О. */
  operatorName:
    "Автономная некоммерческая организация развития социально-психологической " +
    "устойчивости и благополучия граждан «Стратегии возможностей»",
  /** Краткое название школы для текстов документов. */
  brandName: "Профиммунитет",
  inn: "5405073662",
  ogrn: "1225400020515",
  /** Юридический адрес (он же адрес для запросов субъектов ПД). */
  address: "630084, г. Новосибирск, ул. Авиастроителей, д. 19, кв. 65",
  /** Почта для обращений по персональным данным. */
  privacyEmail: "info@profimmunitet.ru",
  /** Почта поддержки (на неё уходят обращения с формы). */
  supportEmail: "info@profimmunitet.ru",
  phone: "+7 913 388 79 60",
  /** Сайт/домен площадки. */
  site: "https://profimmunitet.ru",

  /**
   * Версии документов. Меняйте дату при правке текста: она сохраняется
   * пользователю в момент согласия и служит доказательством того,
   * какую редакцию он принял.
   */
  privacyVersion: "2026-07-24",
  termsVersion: "2026-07-24",
  consentVersion: "2026-07-24",
};

/** Есть ли незаполненные реквизиты — для предупреждения на страницах. */
export function hasPlaceholders(): boolean {
  return Object.values(legal).some((v) => v === NOT_SET);
}

/** Значение или заметная заглушка. */
export function field(value: string): string {
  return value === NOT_SET ? "________________" : value;
}
